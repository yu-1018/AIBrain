# 给 AI 的指令词（记忆导出专用）

> 这个文件是整个仓库里**唯一一份**记忆导出提示词，手机和电脑都用它。完整流程与各平台操作要点见 `adapters/memory-import.md`。
> 用法：在 AI 的对话框里新建一个对话，把第一段整个复制发过去。它输出完，把结果存成 `.md` 丢进 `inbox/`，再跑 `python tools/ingest.py`。
> 太长被截断时，回一句「继续」即可。
> **DeepSeek 是特例**：它没有记忆库，必须翻回原来那个对话窗口再发，见下面「手机版 DeepSeek 专用」。

---

## 第一段：一次性发送（主指令）

```
请你把你知道的关于我的一切，整理成一份清单交给我，我要存进自己的长期资料库。

严格按下面的规则写：

1. 每条用「- 」开头，一条只说一件事，用大白话，不要客套话、不要解释、不要举例。
2. 只写你确实知道的。你记不准、或者可能已经过时的，单独放到最后一节。
3. 严格按我给的 6 个小节输出，标题原样照抄，不要改名、不要合并、不要自己加小节。
4. 每节至少 3 条，确实没有就写「- 暂无」。

## 一、我是谁（基本信息）
- 我的城市：
- 我的职业或身份：
- 我在什么行业：
- 我常用的工具和平台：
- 其他关于我的固定信息：

## 二、我的偏好和习惯
- 我喜欢的沟通风格：
- 我喜欢什么样的输出格式：
- 我不喜欢或讨厌的：
- 我希望你以后记住的习惯：

## 三、重要的人
- 提到过的人：称呼 + 关系 + 对方在意什么

## 四、我正在做和待办的事
- 我正在做的项目：
- 我的待办事项：
- 进行中的计划：

## 五、我定过的主意和原因
- 我决定或选择了什么，以及当时的原因：

## 六、你不确定或可能过时的部分
- 

如果内容太长一次写不完，先写前面几节，等我回复「继续」再写剩下的。
```

---

## 第二段：发给豆包时追加（它可能有你上传过的文件）

```
另外单独回答一个问题：我在你这里上传过、或者存过哪些文件或资料？
请列成清单，每行写「文件名 + 大概内容 + 我的用途」，没有就回「没有」。
```

---

## 第三段：如果它说「我没有记忆功能」

```
那就不要谈记忆。请只根据我们过去所有的对话内容，把你能推断出的关于我的信息，按上面 6 个小节的格式整理给我。只能说推断，不要编造。
```

---

## 手机版 DeepSeek 专用

**先花 30 秒理解它的脾气**（2026-09 核实，多个来源一致）：

DeepSeek **没有「记忆库」这个页面**——没有记忆开关、没有自定义指令框、也没有「查看已记住的条目」。它只在**同一个对话窗口内**记得你说过的话；你只要不删那个窗口，它就全记得；一旦开新窗口，它对你一无所知。

所以「提取它的记忆」正确做法只有一条：**回到你聊得最多、最重要的那个对话窗口**，把下面这段发过去，让它自己总结。

**做的步骤**：

1. 打开 DeepSeek App，点左上角那两条横杠，在历史对话里翻出你最常聊的那个窗口（就是你跟它聊学习规划、项目、日常最多的那个）。
2. 把下面第一段整个复制，发过去。
3. 它输出完，长按它的回复 → 复制 → 先存进手机备忘录（或给自己发一条消息留档），回头在电脑上另存成一个 `.md` 文件丢进 `AIBrain\inbox\`。
4. 回到电脑跑 `python tools\ingest.py --dry-run` 看一眼，没问题再去掉 `--dry-run` 正式导入。

**如果你的记忆分散在好几个窗口**：每个窗口都发一遍第一段，各自存成一个文件（比如 `deepseek-1.md`、`deepseek-2.md`）一起丢进 `inbox\`。导入脚本会自动去掉重复条目，原来已有的内容不会被覆盖，所以多存几份没有坏处。

### 第一段：主指令（一次性发送）

```
请你做一件事：把我们这个对话窗口里聊过的、关于我的长期信息，整理成一份清单给我，我要存进自己的资料库里长期用。

严格按下面的规则写：

1. 一条一行，用「- 」开头，一条只说一件事，用大白话，不要客套话、不要解释、不要举例。
2. 只整理「长期有效」的信息。今天临时聊到的一句话、一次性的闲聊、你觉得不重要的，都不要写。
3. 只写这个对话里我确实说过的。凡是你的推测，统一放最后一节，标明「推测」。
4. 严格按我给的 6 个小节输出，标题原样照抄，不要改名、不要合并、不要自己加小节。
5. 每节至少 3 条，确实没有就写「- 暂无」。

## 一、我是谁（基本信息）
- 我的城市：
- 我的身份 / 专业 / 职业：
- 我在学的、在做的方向：
- 我常用的工具和平台：
- 其他关于我的固定信息：

## 二、我的偏好和习惯
- 我喜欢的说话方式：
- 我想要什么样的输出格式：
- 我不喜欢或讨厌的：
- 我希望你以后记住的：

## 三、重要的人
- 提到过的人：称呼 + 关系 + 对方在意什么

## 四、我正在做和待办的事
- 我在做的项目 / 任务：
- 我的待办和计划：
- 有截止时间的：

## 五、我定过的主意和原因
- 决定了什么，以及当时的原因：

## 六、你推测或不确定的部分
-

写完 6 个小节后，再加一段单独回答这个问题：这个对话里，我上传过或粘贴过哪些文件、资料、链接？请列成清单，每行写「名称 + 大概内容 + 我拿它干嘛」，没有就写「没有」。

最后，把上面全部内容放进一个 Markdown 代码块里输出，方便我直接复制。
如果一次写不完，先写前面几节，等我回复「继续」再写剩下的。
```

### 第二段：如果它说「我没有记忆功能」

```
不用管有没有记忆功能。请只根据我们这个对话窗口里的聊天记录，把你能看出来的关于我的信息，按上面 6 个小节整理给我。只看对话里真实出现过的内容，不要编造，不确定的放到第六节。
```

### 第三段：可选，让它反过来采访你（清单补全用）

如果它总结出来的东西太少——正常，因为对话里本来就没说全。这时候用这段，让它主动问你：

```
现在请你当采访者。为了把上面这份清单补全，你还缺哪些关键信息？一次问我 3 个问题，等我回答完，再问下 3 个。全部问完后，把完整清单重新输出一遍给我。
```

回答它的问题时，尽量说长期有效的事实（身份、偏好、目标、在做的事），别聊一次性的话题。

### 手机上的操作要点

- 复制它的长回复：长按那段文字 → 全选 → 复制。太长不好选就点回复右下角的复制按钮（有的话）。
- 存文件：粘到备忘录，或者发给微信里的「文件传输助手」，到电脑上再另存为 `.md`。
- 懒得存文件也行：直接粘进 AIBrain 网页版（本地跑起来的那个编辑页面）右侧的编辑框，存进 `inbox/`。
- 一个窗口发一遍就够，反复发只是把同样的东西重复输出，没有额外收获。

---

## 拿到结果之后怎么处理

1. 把 AI 输出的内容**原样**存成一个 `.md` 或 `.txt` 文件，丢进 `inbox/`（不用整理、不用排版，脚本会自己分类）。
2. 先预览：`python tools/ingest.py --dry-run`
3. 确认无误后正式导入：`python tools/ingest.py`

导入时的自动分类规则：

| 内容类型 | 去向 |
| --- | --- |
| 基本信息 | `memory/profile.md` |
| 偏好与习惯 | `memory/preferences.md` |
| 重要的人 | `memory/people.md` |
| 待办与进行中的事 | `memory/commitments.md` |
| 已定决策与理由 | `memory/decisions.md` |
| 纯知识性内容 | `knowledge/notes/`（并在 `knowledge/index.md` 补一行） |

重复的条目自动去掉，原来已有的内容一律不覆盖；导入完成后会自动重跑编译。

---

## 一句提醒

**不要把这些贴给 AI**：密码、身份证号、银行卡、别人的手机号和住址。这些东西进了清单就会跟着进仓库。
