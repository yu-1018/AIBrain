# 项目资料

> 一个项目一个文件。**重点不是记录做了什么，而是记录那些文档里不会写的东西**：
> 客户到底在意什么、当时为什么排除了另一个方案、谁在什么节点上卡过。

## 命名

`项目名.md`，比如 `客户A品牌站改版.md`。项目名要用你自己认得的叫法，不要用内部编号。

## 起手式

复制 `../templates/project.md`，改名为项目名，然后逐节填。

## 存档规则

项目结束后**不要删**，在文件顶部把状态改为「已归档」，并补上「复盘」一节。这是库里最值钱的部分。
