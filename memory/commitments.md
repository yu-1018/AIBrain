# 进行中的事

> 状态变完的事就删掉或移到 `decisions.md`。这个文件只保留**还没结束**的。

| 事项 | 状态 | 下一步 | 截止 | 更新 |
| --- | --- | --- | --- | --- |

<!-- 在表格里逐行添加新事项 -->

## 状态取值

`待开始` / `进行中` / `阻塞` / `待确认` / `已完成`

## 记录原则

- 「下一步」必须是**动词开头的具体动作**，不要写「跟进一下」。
- 阻塞的事，在状态里写清卡在谁那。
