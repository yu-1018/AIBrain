# 这台电脑的环境事实

> 记的是「这台电脑本身」的硬事实，跟用哪个 AI 平台无关。任何要在这台机器上动手的 AI，先看这几条能少踩坑。
> 更细的排查记录放 `knowledge/notes/` 里，这里只留最常撞上的那几条。

## 机器与路径

- 设备：<系统与配置，例如 Windows 11 家庭版 / 16GB 内存 / 无独立显卡>；时区 Asia/Shanghai。
- 跑本机脚本统一用这个 Python：`<你的 Python 绝对路径>`
- 本机浏览器：<Edge / Chrome 的绝对路径>
- 本机缺什么工具（例如没有 ffmpeg、没有 Pillow），写清楚，免得 AI 反复推荐用不了的办法。

## 一碰就出问题的坑

- <例：代理软件会把 github.com 劫持到本地，抓网页要用 curl.exe --ssl-no-revoke>
- <例：命令行带管道会卡住，改用 Python> 
- <例：某个平台默认不执行安装脚本，要加参数>
- 每条写「现象 → 解法」，不要只写现象。

## 账号与凭证（只记位置，不记密码）

- <例：git 凭证已存在系统凭据管理器里，普通 push 不用输密码；密码本身一律不写进这个仓库>
